This folder contains developer tooling for generating simplified TypeScript types from our Zod schemas.

Usage:

- Install dev deps: npm ci
- Run: npm run generate-types

This will write dist/esm/generated-types.d.ts and dist/cjs/generated-types.d.ts
